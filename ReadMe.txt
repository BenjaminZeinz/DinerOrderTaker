Just run the 'build.bat' and the following will occur:
    compile the 3 projects for DotNet Core 2.1 (library, console, and unit tests)
    run the 15 unit tests, 
    and then run the console program to accept manual inputs.